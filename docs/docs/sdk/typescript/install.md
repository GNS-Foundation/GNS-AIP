# Installation

```bash
npm install @gns-aip/sdk
# or
yarn add @gns-aip/sdk
# or
pnpm add @gns-aip/sdk
```

**Requirements:** Node.js >= 18, TypeScript >= 5.0 (recommended). Zero runtime dependencies.
