# Installazione

```bash
npm install @gns-aip/sdk
# oppure
yarn add @gns-aip/sdk
# oppure
pnpm add @gns-aip/sdk
```

**Requisiti:** Node.js >= 18, TypeScript >= 5.0 (consigliato). Zero dipendenze runtime.
