{
  "name": "gns-aip",
  "version": "0.1.0",
  "private": true,
  "description": "GNS Agent Identity Protocol — Proof-of-Jurisdiction for AI agents",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/sdk && npm run build -w packages/langchain && npm run build -w packages/crewai",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GNS-Foundation/GNS-AIP"
  },
  "author": "GNS Foundation",
  "license": "Apache-2.0",
  "dependencies": {
    "@langchain/core": "^1.1.30",
    "langsmith": "^0.5.7"
  }
}
